# 🤖 Boto URL-сокращение

Мини-приложение на **Python 3.12** для сокращения ссылок с историей запросов.

## 🚀 Как запустить

### 1. Клонирование и подготовка
Вытащите проект из репозитория и перейдите в папку (назодись в консоли там куда будешь тащить приложение):

`git clone git@github.com:xash1rama/test_boto.git`


### 2. Установка зависимостей
1) Перейдите в папку с приложением:

   `cd BOTO_ED`

3) Создайте и активируйте окружение:
   
   `python3 -m venv .venv`
   
   `source .venv/bin/activate` # Для macOS/Linux

4) Обновите pip(при необходимости):
   
   `pip install --upgrade pip`
   
5) Перейдите в папку с "pyproject.toml":
   
   `cd boto_test`

6) Установите проект и зависимости:
 
  `pip install .`

### 3. Запуск
Приложение автоматически создаст базу данных **links.db** в корневой папке при первом запуске. Никаких дополнительных настроек `.env` не требуется.

`uvicorn main:app --reload`

Зайди в браузере на  `http://127.0.0.1:8000` или по url  в консоли

## 🛠 Как тестировать

### 1. Находится в нужном каталоге

Нужно находиться в рабочей дериктории "BOTO_ED/boto_test" 

### 2. Запуск тестов

Запустить команду:

`python3 -m pytest -v`






